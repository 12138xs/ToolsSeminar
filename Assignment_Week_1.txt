使用正则表达式对字符串进行匹配,在符合条件用户名前存在(#),则须判断Positive LookBehind为符号')',
而Positive LookAhead为符号'('或者\s字符,其中第一个字母为小写,则添加[a-z]用于判断第一个字符字符,
字符串长5到20,则剩余4到19个字符,这些字符可以是大小写字母和下划线,则设置[a-zA-Z_]{4,19},匹配全文
所有字符串,则需要/g,则总语句为/(?<=[)])[a-z][a-zA-Z_]{4,19}(?=[(|\s]/gm).对于FLAVOR的选择，使用
PHP,Python会超时,使用Golang会模式错误，使用Javascript可顺利运行,且时间在40ms左右
